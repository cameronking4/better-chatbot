apiVersion: v1
kind: Service
metadata:
  name: better-chatbot-service
  labels:
    app: better-chatbot
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: better-chatbot
